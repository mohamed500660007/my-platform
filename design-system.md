# نظام تصميم «بــن نــاجــح»
### Ben Nagah Design System — Theme · Colors · Fonts

نظام التصميم المستخدم في:

| الملف | الوصف |
|---|---|
| `lesson-1-1.html` | كتاب الشرح — ٨ صفحات A4 |
| `questions-1-1.html` | كتاب الأسئلة — غلاف + ١٩ صفحة A4 |
| `presentation-1-1.html` | العرض التقديمي — ٢٦ شريحة 16:9 |

**الفكرة الأساسية:** ورقة مذكّرة حقيقية — ورق عاجي دافئ، حبر نيلي، أقلام تحديد فسفورية، شرايط واشي تيب، وميلان بسيط في كل عنصر عشان يبان مكتوب بالإيد مش مطبوع بالكمبيوتر.

---

## ١ · الألوان

### ١.١ الورق والحبر (Neutrals)

| التوكن | الكود | الاستخدام |
|---|---|---|
| `--paper` | `#FFFCF2` | خلفية الورقة الأساسية |
| `--paper-2` | `#FBF5E4` | نهاية تدرّج الورقة (لأسفل) |
| `--rule` | `#DCE6F2` | خطوط الشبكة في الرسوم البيانية |
| `--ink` | `#1E2A46` | لون النص الأساسي · الحدود · الخلفيات الداكنة |
| `--ink-soft` | `#5C6683` | نص ثانوي · شروح · تعليقات |
| `--ink-faint` | `#98A0B6` | نص خافت · تسميات الرسوم · أرقام الصفحات |
| `--desk` | `#C9C4B4` | المكتب اللي تحت الورقة |
| `--desk-2` | `#B7B2A1` | نهاية تدرّج المكتب · لون الخروم |

**قاعدة:** خلفية الورقة دايمًا تدرّج قطري من `--paper` لـ `--paper-2`، وفوقه شبكة نقطية:

```css
background:
  radial-gradient(circle at 1px 1px, rgba(30,42,70,.055) 1px, transparent 1.6px) 0 0/5mm 5mm,
  linear-gradient(180deg, var(--paper) 0%, var(--paper-2) 100%);
```

### ١.٢ أقلام التحديد الخمسة (Accents)

كل قلم له **لون فاتح** (للخلفيات والحدود) و**حبر غامق** (للنص فوق اللون الفاتح).

| القلم | اللون | الحبر | التنت الفاتح | الدور في النظام |
|---|---|---|---|---|
| **Sun** ☀️ | `#FFC93C` | `#6B4B00` | `#FFF0C4` | تنبيه · تركيز · رقم الصفحة · الاسم في الشريط |
| **Berry** 🍓 | `#FF4D6D` | `#7A0C24` | `#FFE1E7` | خطأ · تحذير · الفخّ · التمييز في العناوين |
| **Mint** 🌿 | `#00C2A8` | `#005247` | `#D6F7F1` | صح · إجابة نموذجية · نشاط |
| **Violet** 🔮 | `#7A5CFF` | `#2E1C7A` | `#E9E2FF` | مصطلحات · مفاهيم · الأرقام الشبح |
| **Sky** 💧 | `#2FA8FF` | `#0A4C7D` | `#DCEEFF` | معلومة · ربط · رسوم بيانية |

> **قاعدة التتابع:** الأقلام بتتوزّع بالترتيب ده على العناصر المتكرّرة (`nth-child(5n+1)` … `5n+5`) عشان أي قايمة تطلع متوازنة لونيًّا من غير اختيار يدوي.

### ١.٣ التنتات الإضافية

درجات أفتح بتستخدم كخلفيات للكروت والملاحظات:

| اللون | الكود | مع أي قلم |
|---|---|---|
| `#FFECF0` · `#FFEDF1` | ورديّ فاتح جدًا | Berry — الملاحظات |
| `#E8F4FF` · `#E6F3FF` | سماوي فاتح جدًا | Sky — الملاحظات |
| `#E4FAF6` | نعناعي فاتح جدًا | Mint — الملاحظات والإجابات |
| `#EFEAFF` · `#EDE7FF` | بنفسجي فاتح جدًا | Violet — الشارات |
| `#FFF4D6` · `#FFF4D9` · `#FFFDF6` | كريمي دافئ | Sun — كروت التنبيه |
| `#F2F1EC` · `#D8D6CE` | رمادي محايد | الخيار «القديم» في المقارنات |
| `#FCFAF3` · `#FFFDF4` · `#FDF6DF` | ورق أفتح | صناديق الأسئلة والقصاصات |
| `#03332C` | أخضر داكن جدًا | نص فوق خلفية Mint صريحة |
| `#4A3200` | بنّي داكن | رقم الصفحة فوق خلفية Sun |

### ١.٤ ألوان العلامات التجارية (ثابتة — ممنوع تغييرها)

| المنصة | الكود | ملاحظة |
|---|---|---|
| YouTube | `#FF0033` | أيقونة مربّعة `border-radius:4–5px` |
| Facebook | `#1877F2` | أيقونة دائرية |
| WhatsApp | `#25D366` | أيقونة دائرية · لون الأيقونة جوّه `#08351A` |

### ١.٥ ألوان خاصة بالعرض التقديمي

| العنصر | القيمة |
|---|---|
| خلفية المسرح | `radial-gradient(120% 90% at 50% 0%, #1E2532 0%, #14181F 60%, #0E1116 100%)` |
| تدرّج شريط الفوتر | `linear-gradient(178deg, #33426A 0%, var(--ink) 58%)` |
| الخط الملوّن فوق الشريط | `linear-gradient(90deg, var(--berry), var(--sun) 38%, var(--mint) 68%, var(--sky))` |
| تدرّج الدايرة (Burst) | `radial-gradient(circle at 42% 34%, #FFE9A6 0%, var(--sun) 58%, #FFAA10 100%)` |

---

## ٢ · الخطوط

### ٢.١ التحميل

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lalezar&family=Rakkas&family=Marhey:wght@300..700&family=Tajawal:wght@300;400;500;700;800&family=Aref+Ruqaa:wght@400;700&display=swap">
```

### ٢.٢ الأدوار — كل خط له وظيفة واحدة بس

| الخط | الأوزان | الدور | أمثلة |
|---|---|---|---|
| **Rakkas** | 400 | العناوين الكبيرة · اللوجو · الأرقام الشبح | `h1` · `.lockup` · `.ghost` · `.fmark` |
| **Lalezar** | 400 | عناوين الأقسام · الشرائط · الأرقام البارزة | `.ribbon` · `.era h5` · `.gterm b` · `.fpage b` |
| **Marhey** | 300–700 | تسميات الواجهة · الشارات · الأزرار | `.eyebrow` · `.lbl` · `.impact` · `.chip` |
| **Tajawal** | 300–800 | نص المحتوى كله | `p` · `li` · `td` · `.answer` |
| **Aref Ruqaa** | 400, 700 | الاقتباس الختامي **فقط** | `.pullquote q` |

**سلسلة الاحتياط:**
```css
font-family: "Tajawal", "Segoe UI", Tahoma, sans-serif;   /* المتن */
font-family: "Rakkas", "Lalezar", serif;                  /* العناوين */
font-family: "Lalezar", cursive, sans-serif;              /* الشرائط */
```

> ⚠️ **Rakkas و Lalezar لهما وزن واحد (400).** لو كتبت `font-weight:700` عليهم المتصفح هيعمل تغليظ صناعي وحش. استخدم `-webkit-text-stroke` أو `text-shadow` للتقوية بدلًا منه.

> ⚠️ **`line-height` لخط Rakkas لازم ≥ 1.2** — حروفه بتعلى وتنزل بره صندوق السطر. لو حطّيته `1` جوّه عنصر فيه `overflow:hidden` هيتقصّ. الاسم في شريط الفوتر بيستخدم `line-height:1.8`.

### ٢.٣ مقياس الطباعة — A4 (بالـ pt)

الورقة `210×297mm` بحشو `15mm 24mm 13mm 16mm`.

| العنصر | المقاس | ارتفاع السطر | الخط |
|---|---|---|---|
| `.title` | `33pt` | 1.04 | Rakkas |
| `.title.long` | `26.5pt` | 1.1 | Rakkas |
| `.ghost` (رقم شبح) | `88pt` | 1 | Rakkas · `stroke 2.4px` |
| `.ribbon` | `14pt` | 1.3 | Lalezar |
| `.subtitle` | `10.5pt` | — | Marhey |
| `.lede` | `10.8pt` | 1.52 | Marhey |
| `p` (المتن) | `9.6pt` | 1.5 | Tajawal · `max-width:66ch` |
| `.callout p` | `9.2pt` | 1.48 | Tajawal |
| `.aside-note` | `8.2pt` | 1.45 | Marhey |
| `.trap .th` | `11.5pt` | 1.25 | Lalezar |
| `.askform q` | `8.9pt` | 1.5 | Tajawal · `700` |
| `.fmark` (الاسم) | `19pt` | **1.8** | Rakkas |

### ٢.٤ مقياس الشاشة — العرض (بالـ px على مسرح 1920×1080)

| العنصر | المقاس | ارتفاع السطر |
|---|---|---|
| `h1.t` | `100px` | 1.22 (الغلاف 1.46) |
| `h2.t` | `84px` | 1.2 |
| `h2.t.sm` | `70px` | 1.2 |
| `.ribbon` | `38px` | 1.3 |
| `.eyebrow` | `25px` | — |
| `p.big` | `34px` | 1.55 |
| `p.mid` | `30px` | 1.6 |
| `p.sml` | `25px` | 1.55 |
| `.trap .th` | `42px` | 1.25 |
| `.lockup .w2` | `150px` | 1.26 |

---

## ٣ · ملامح النظام (Design Language)

الحاجات اللي بتخلّي التصميم يبان «مكتوب بالإيد» مش «مولَّد».

### ٣.١ الميلان الدقيق
كل عنصر بارز بياخد ميلان بين **‎-0.3deg‎ و ‎-1.2deg‎** — كبير كفاية إنه يحسّ بالحياة، صغير كفاية إنه ما يبانش غلط.

```css
transform: rotate(-.5deg);    /* الملاحظات */
transform: rotate(-.9deg);    /* الشرائط */
transform: rotate(-1.2deg);   /* شارة الهوية */
transform: rotate(-7deg);     /* الأختام والدوائر */
```

### ٣.٢ الزوايا العضوية
مش `border-radius` متساوي — قيم مختلفة على كل ركن عشان تبان مرسومة:

```css
border-radius: 26px 8px 24px 10px / 20px 26px 10px 22px;  /* الملاحظة */
border-radius: 15px 5px 15px 5px;                          /* كارت التنبيه */
border-radius: 13px 5px 13px 5px;                          /* بطاقة المصطلح */
```

### ٣.٣ الظل الصلب (Hard Shadow)
**مفيش blur.** الظل إزاحة صريحة بلون الحبر الشفّاف — يدّي إحساس الطباعة والستيكر:

```css
box-shadow: 4px 5px 0 rgba(30,42,70,.14);    /* كارت */
box-shadow: 10px 12px 0 rgba(30,42,70,.15);  /* كارت كبير */
box-shadow: inset 0 0 0 2px var(--ink);      /* الحدّ الداخلي */
```

### ٣.٤ قلم التحديد (Highlighter)
تدرّج مايل `100deg` بيبدأ وينتهي شفّاف — زي ضغطة القلم الحقيقية:

```css
.hl{
  background-repeat:no-repeat;
  background-size:100% .78em;
  background-position:0 .62em;
  padding:0 .18em;
  font-weight:700;
  -webkit-box-decoration-break:clone; box-decoration-break:clone;
}
.hl-sun{ background-image:linear-gradient(100deg,
   rgba(255,201,60,0) 0 1%, rgba(255,201,60,.85) 1% 99%, rgba(255,201,60,0) 99%); }
```
الشفافيات: Sun `.85` · Mint `.42` · Berry `.34` · Sky `.34`

### ٣.٥ الشريط المنشور (Ribbon)
عنوان القسم بأطراف مشقوقة:

```css
clip-path: polygon(0 0, 100% 0, calc(100% - 13px) 50%, 100% 100%, 0 100%, 13px 50%);
```

### ٣.٦ الواشي تيب
شرايط لاصقة بتخرج بره حدود الورقة، بأربع نقشات:

```css
.stripe-sun { background:repeating-linear-gradient(-58deg, var(--sun) 0 7px, #FFE49A 7px 14px); }
.stripe-mint{ background:repeating-linear-gradient(-58deg, var(--mint) 0 6px, #9DF0E4 6px 12px); }
.dot-berry  { background:radial-gradient(circle at 4px 4px, rgba(255,255,255,.85) 1.8px, transparent 2px) 0 0/9px 9px, var(--berry); }
.plain-sky  { background:var(--sky); }
```
كل شريط له لمعة علوية `linear-gradient(180deg, rgba(255,255,255,.42), transparent 55%)`.

### ٣.٧ الدائرة المرسومة (Circled)
تحويطة بيضاوية غير منتظمة حوالين كلمة:

```css
border-radius: 52% 48% 45% 55% / 58% 42% 58% 42%;
border: 2.2px solid var(--berry);
transform: rotate(-1.6deg);
```

---

## ٤ · الحركة (خاص بالعرض التقديمي)

### ٤.١ منحنيات التوقيت

| التوكن | القيمة | الاستخدام |
|---|---|---|
| `--ease-out` | `cubic-bezier(.16,1,.3,1)` | الدخول — فرملة ناعمة |
| `--ease-in` | `cubic-bezier(.55,0,.65,1)` | الخروج — تسارع |
| `--ease-pop` | `cubic-bezier(.34,1.56,.64,1)` | الارتداد — للأختام والشارات |

### ٤.٢ أنواع الدخول الثمانية

| `data-anim` | الحركة |
|---|---|
| `rise` | من تحت + تكبير خفيف |
| `drop` | من فوق |
| `left` / `right` | من الجنب |
| `pop` | تكبير من `.7` مع ميلان |
| `tilt` | من تحت مع دوران ‎-6deg‎ |
| `wipe` | كشف بـ `clip-path` من اليمين للشمال |
| `stamp` | ختم: يدخل مكبّر ٪١٥٥ ومايل ‎-9deg‎ وبيترجّع |

المدد: العناصر `.78s` · انتقال الشريحة داخل `.78s` / خارج `.62s` · الكشف `.95s`.

### ٤.٣ انتقال الشرائح (RTL)
الشريحة الجاية بتدخل **من الشمال** بعمق ثلاثي الأبعاد، والرايحة بتخرج **ناحية اليمين**:

```css
@keyframes inFwd{
  from{ opacity:0; transform:translate3d(-13%,0,-420px) rotateY(11deg) scale(.94); filter:blur(9px); }
  to  { opacity:1; transform:none; filter:blur(0); }
}
```

### ٤.٤ إتاحة الوصول
```css
@media (prefers-reduced-motion:reduce){
  /* كل الحركة بتتلغي */
}
```

---

## ٥ · قواعد التخطيط

### ٥.١ الطباعة (الكتب)

```css
@page{ size:A4; margin:0; }
@media print{
  *,*::before,*::after{
    -webkit-print-color-adjust:exact !important;
    print-color-adjust:exact !important;
  }
  .sheet{ width:210mm; height:296.8mm; break-after:page; break-inside:avoid; }
}
```

| القياس | القيمة |
|---|---|
| مقاس الورقة | `210mm × 297mm` |
| الحشو | `15mm` فوق · `24mm` يمين · `13mm` تحت · `16mm` شمال |
| خط الهامش الأحمر | `right: 19mm` |
| خروم الأجندة | `right: 6mm` · قطر `6mm` · فاصل `42mm` |
| **أقصى ارتفاع للمحتوى** | **‎~262mm‎** (بعدها بيصطدم بشريط الفوتر) |

### ٥.٢ العرض التقديمي

| القياس | القيمة |
|---|---|
| المسرح | `1920 × 1080` (16:9) |
| حشو الشريحة | `78px 116px 104px` |
| التوسيط | `position:absolute; top:50%; left:50%` + `translate(-50%,-50%) scale(k)` |
| معامل التحجيم | `k = min(vw/1920, vh/1080)` |

> ⚠️ **ما تستخدمش `display:grid; place-items:center` لتوسيط المسرح.** لما الشاشة تبقى أضيق من `1920px` المتصفح بيزحلق العنصر بره الشاشة. لازم الإحداثيات الصريحة.

### ٥.٣ نقطة الكسر

`@media screen and (max-width: 820px)` — على الشاشات الضيقة:
- الورقة `min-height:0` → **فشريط الفوتر لازم يبقى `position:static`** وإلا هيركب على المحتوى.
- الشبكات بتتحوّل لعمود أو عمودين.
- العرض التقديمي بيعرض لافتة «لفّ الموبايل بالعرض».

---

## ٦ · المكوّنات الأساسية

| المكوّن | الكلاس | الوصف |
|---|---|---|
| الورقة | `.sheet` | صفحة A4 كاملة |
| الشريحة | `.slide` | شريحة 16:9 |
| الشريط المنشور | `.ribbon` + `.r-{color}` | عنوان قسم |
| الملاحظة | `.callout` + `.c-{color}` | حقل لون بشارة دائرية بارزة |
| الفكرة الكبرى | `.bigidea` | خلفية حبر + لمبة صفرا |
| تنبيه المعلّم | `.trap` | ترويسة داكنة + جسم + `.askform` + `.trapline` |
| الخط الزمني | `.timeline` / `.tl` | عمود متدرّج + حقب |
| المصطلح | `.gterm` | عربي + إنجليزي + تعريف |
| المقارنة | `.edge` + `.lane-a/-b` | مسارين وبينهم دايرة «ضد» |
| صندوق السؤال | `.qbox` | حدّ داخلي `2px` + ظل صلب |
| شريط الهوية | `.fbar` | اسم الكتاب · الاسم الممطوط · رقم الصفحة |
| صورتك | `.photo` / `.avatar` | إطار دائري بحدّ حبر وظل ملوّن |

### ٦.١ الاسم الممطوط — الكشيدة

المطّ بالكشيدة العربية `U+0640` مش بـ `letter-spacing`، عشان الحروف تفضل موصولة:

```
بــــــن نــــــاجــــــح
```
٦ كشيدات في كل وصلة قابلة للمطّ: `بـ→ن` · `نـ→ا` · `جـ→ح`.
**ما تحطش كشيدة بعد الألف** — الألف مبتوصلش لقدّام.

مع شدّ أفقي خفيف: `transform: scaleX(1.12)`

### ٦.٢ صورتك

متغيّر واحد بيتحكّم في كل أماكن الصورة:

```css
--photo: url("my-photo.jpg");
```
حطّ الصورة جنب ملف الـHTML بنفس الاسم، أو غيّر الاسم هنا. الأماكن: غلاف كتاب الأسئلة · غلاف العرض · شريحة النهاية · الأفاتار في شريط الفوتر.

---

## ٧ · ملخّص التوكنز — انسخه والصقه

```css
:root{
  /* الورق والحبر */
  --paper:      #FFFCF2;
  --paper-2:    #FBF5E4;
  --rule:       #DCE6F2;
  --ink:        #1E2A46;
  --ink-soft:   #5C6683;
  --ink-faint:  #98A0B6;
  --desk:       #C9C4B4;
  --desk-2:     #B7B2A1;

  /* أقلام التحديد */
  --sun:        #FFC93C;  --sun-ink:    #6B4B00;
  --berry:      #FF4D6D;  --berry-ink:  #7A0C24;
  --mint:       #00C2A8;  --mint-ink:   #005247;
  --violet:     #7A5CFF;  --violet-ink: #2E1C7A;
  --sky:        #2FA8FF;  --sky-ink:    #0A4C7D;

  --shadow: 0 1px 2px rgba(30,42,70,.06), 0 12px 28px rgba(30,42,70,.10);
  --photo:  url("my-photo.jpg");

  /* الحركة (العرض التقديمي) */
  --ease-out: cubic-bezier(.16,1,.3,1);
  --ease-in:  cubic-bezier(.55,0,.65,1);
  --ease-pop: cubic-bezier(.34,1.56,.64,1);
}
```

---

## ٨ · قواعد ما تكسرهاش

1. **ما تزوّدش لون سادس.** الخمس أقلام كفاية — لو محتاج تمييز جديد استخدم تنت أفتح من قلم موجود.
2. **الميلان يفضل تحت ‎1.5deg‎** للكروت والنصوص. اللي فوق كده للأختام بس.
3. **الظل صلب دايمًا** (`0` blur). أي `blur` في الظل بيكسر إحساس الطباعة.
4. **Rakkas و Lalezar وزن 400 بس.**
5. **`line-height` لـ Rakkas ≥ 1.2** جوّه أي حاوية فيها `overflow:hidden`.
6. **نص المتن Tajawal، والتسميات Marhey.** ما تخلطهمش.
7. **العناصر الـabsolute جوّه ورقة `min-height:0`** لازم تبقى `static` في نسخة الموبايل.
8. **الاتجاه RTL** — استخدم `right`/`left` بوعي: `margin-right:auto` في flex بيدفع للشمال.

---

*آخر تحديث: ١ سبتمبر ٢٠٢٦ — القيم كلها مستخرَجة من الملفات الثلاثة مباشرة.*
